local function run()
	print("List of StudyTracker commands")
	print("")
	print("\thelp - shows this command.")
	print("\texit - kills the current process (exit the program).")
end

return { run = run }
